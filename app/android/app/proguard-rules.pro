# Project-specific R8 rules (7.5). Most Flutter plugins ship their own
# consumer-rules.pro and don't need entries here. Add rules only if a
# release build crashes/misbehaves in a way `flutter analyze`/tests don't
# catch (reflection-based libraries are the usual culprit).
